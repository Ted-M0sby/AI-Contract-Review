# 合同审查系统项目汇报

## 一、项目整体说明

本项目是一个面向房屋租赁合同的在线审查系统。用户通过前端完成 QQ 邮箱注册、登录、合同上传、合同详情查看、发起 AI 审查、查看审查结果，并可在审查完成后发送钉钉人工复核提醒。

项目后端核心文件是 `backend/app.py`，它使用 FastAPI 统一提供接口服务，负责用户认证、验证码邮件、合同文件接收、合同文本解析、合同数据存储、审查任务发起、审查结果读取、审查完成邮件通知和钉钉推送。前端核心是 Vue 3 页面和 jQuery 请求封装，所有业务请求通过 `src/api/request.js` 里的 `$.ajax` 发包，再由 `src/api/auth.js`、`src/api/contract.js` 封装成页面可直接调用的函数。

整体链路可以概括为：

1. 用户在浏览器操作 Vue 页面。
2. 页面调用前端 API 封装函数。
3. 前端 API 封装函数统一走 jQuery `$.ajax`。
4. 开发环境下请求先发到 Vite 的 `/api` 代理。
5. Vite 将 `/api` 请求转发到 FastAPI 后端 `127.0.0.1:8888`。
6. `backend/app.py` 根据接口逻辑读写 MySQL、保存上传文件、解析合同文本、调用外部 AI 审查能力、发送邮件或钉钉消息。
7. 后端返回统一 JSON，前端根据 `code` 字段判断成功或失败并更新页面状态。

说明：本文只围绕本项目自身实现说明，审查能力对应的外部平台不展开介绍，统一称为“外部 AI 审查服务”。

## 二、后端架构与 app.py 结构

`backend/app.py` 是一个单文件 FastAPI 后端，主要由以下部分组成：

1. 依赖导入：包含 FastAPI、Pydantic、MySQL、文件解析、邮件发送、HTTP 请求、环境变量加载等依赖。
2. 环境变量加载：启动时通过 `.env` 读取邮箱、AI 审查服务、钉钉 webhook 等配置。
3. FastAPI 应用实例：`app = FastAPI()`。
4. CORS 跨域配置：允许前端跨域调用后端接口，支持所有来源、方法、请求头和响应头。
5. MySQL 连接函数：`mysql()` 统一创建数据库连接。
6. 邮件通知函数：`send_review_done_email()` 在合同审查完成后向用户发送 HTML 邮件。
7. Pydantic 请求模型：定义认证、审查、钉钉推送等 JSON 请求体结构。
8. 业务接口：认证接口、合同接口、审查接口、钉钉接口。
9. 本地启动入口：直接运行 `app.py` 时使用 Uvicorn 启动服务，监听 `0.0.0.0:8888`。

### 2.1 后端运行入口

文件底部的启动配置为：

```python
uvicorn.run(
    "app:app",
    host="0.0.0.0",
    port=8888,
    reload=True
)
```

这表示后端服务默认运行在 `8888` 端口。前端开发服务器的代理也指向这个端口，因此开发环境下前端访问 `/api/...`，最终会被代理到 `http://127.0.0.1:8888/...`。

### 2.2 跨域配置

后端通过 `CORSMiddleware` 允许前端访问接口：

- `allow_origins=["*"]`：允许任意来源。
- `allow_methods=["*"]`：允许所有 HTTP 方法。
- `allow_headers=["*"]`：允许所有请求头。
- `allow_credentials=True`：允许携带凭证。
- `expose_headers=["*"]`：允许前端读取响应头。

这个配置适合本地演示和课程项目。它降低了前端开发调试成本，避免 Vue 开发服务器和 FastAPI 后端端口不一致导致跨域失败。

### 2.3 数据库连接

后端通过 `mysql()` 函数连接 MySQL：

- 地址：`127.0.0.1`
- 端口：`3306`
- 数据库：`contract`
- 用户：`root`
- 字符集：`utf8mb4`
- 游标类型：`DictCursor`

`DictCursor` 的作用是让查询结果以字典返回，例如 `user['email']`、`contract['status']`，这样接口代码可读性更好。

从代码使用情况看，后端主要依赖三类表：

- `users`：用户账号表，保存 `id`、`email`、`password` 等字段。
- `email_codes`：邮箱验证码表，保存 `email`、`code`、`created_at` 等字段。
- `contracts`：合同主表，保存合同元信息、上传文件路径、解析文本、审查状态、审查结果、错误信息和审查时间等字段。

### 2.4 请求模型

`app.py` 使用 Pydantic 定义请求体模型，接口入参结构比较清晰：

| 模型 | 字段 | 使用接口 |
| --- | --- | --- |
| `EmailData` | `email: str` | 发送验证码 |
| `RegisterData` | `email: str`、`password: str`、`code: str` | 注册 |
| `LoginData` | `email: str`、`password: str` | 登录 |
| `ReviewData` | `user_id: int`、`review_perspective: str = "neutral"` | 发起合同审查 |
| `DingTalkData` | `user_id: int` | 发送钉钉复核提醒 |

## 三、后端接口详细说明

### 3.1 发送注册验证码

接口：

```http
POST /auth/send-code
Content-Type: application/json
```

请求体：

```json
{
  "email": "example@qq.com"
}
```

核心作用：给用户 QQ 邮箱发送注册验证码。

处理流程：

1. 校验邮箱后缀，必须以 `@qq.com` 结尾。
2. 生成 6 位随机数字验证码。
3. 连接 MySQL。
4. 删除该邮箱之前的验证码记录，避免同一邮箱存在多个有效验证码。
5. 插入新的验证码到 `email_codes` 表。
6. 提交数据库事务并关闭连接。
7. 从环境变量读取 QQ 邮箱账号和授权码。
8. 组装 HTML 邮件内容。
9. 通过 QQ SMTP SSL 服务发送验证码邮件。
10. 返回发送结果。

成功返回：

```json
{
  "code": 200,
  "message": "验证码发送成功"
}
```

失败返回示例：

```json
{
  "code": 400,
  "message": "请输入QQ邮箱"
}
```

或：

```json
{
  "code": 500,
  "message": "验证码发送失败",
  "error": "具体异常信息"
}
```

这个接口的特点是先写入数据库，再发送邮件。验证码有效期不是在插入时单独写死，而是在注册接口查询时通过 `created_at >= NOW() - INTERVAL 5 MINUTE` 判断 5 分钟内有效。

### 3.2 用户注册

接口：

```http
POST /auth/register
Content-Type: application/json
```

请求体：

```json
{
  "email": "example@qq.com",
  "password": "123456",
  "code": "123456"
}
```

核心作用：校验验证码并创建用户账号。

处理流程：

1. 连接 MySQL。
2. 查询 `users` 表，判断邮箱是否已经注册。
3. 如果邮箱已存在，返回 `邮箱已注册`。
4. 查询 `email_codes` 表中该邮箱最近 5 分钟内最新的一条验证码。
5. 如果验证码不存在或过期，返回 `验证码不存在或已过期`。
6. 比较用户提交的验证码和数据库里的验证码。
7. 如果验证码不一致，返回 `验证码错误`。
8. 向 `users` 表插入新用户。
9. 获取新用户 `user_id`。
10. 删除该邮箱的验证码记录。
11. 返回注册成功信息。

成功返回：

```json
{
  "code": 200,
  "message": "注册成功",
  "user_id": 1,
  "email": "example@qq.com"
}
```

失败返回包括：

- `邮箱已注册`
- `验证码不存在或已过期`
- `验证码错误`

注册接口本身不自动登录，前端注册成功后会跳转回登录页。

### 3.3 用户登录

接口：

```http
POST /auth/login
Content-Type: application/json
```

请求体：

```json
{
  "email": "example@qq.com",
  "password": "123456"
}
```

核心作用：校验邮箱和密码，返回用户身份信息。

处理流程：

1. 连接 MySQL。
2. 根据 `email` 和 `password` 查询 `users` 表。
3. 如果没有匹配用户，返回 `邮箱或密码错误`。
4. 如果匹配成功，返回 `user_id` 和 `email`。

成功返回：

```json
{
  "code": 200,
  "message": "登录成功",
  "user_id": 1,
  "email": "example@qq.com"
}
```

前端收到登录成功后，会把 `user_id` 和 `email` 保存到 `localStorage`，后续合同接口都会从 `localStorage` 读取 `user_id` 作为当前用户身份。

### 3.4 上传合同

接口：

```http
POST /contracts/upload
Content-Type: multipart/form-data
```

表单字段：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `user_id` | int | 是 | 当前登录用户 ID |
| `title` | string | 是 | 合同名称 |
| `contract_type` | string | 是 | 合同类型，目前前端固定为 `housing_lease` |
| `file` | file | 是 | 上传的合同文件 |

核心作用：接收合同文件，解析合同正文，并写入合同表。

处理流程：

1. 根据 `user_id` 查询 `users` 表，确认用户存在。
2. 获取上传文件名。
3. 根据文件扩展名判断类型，只允许 `pdf`、`docx`、`txt`。
4. 如果 `uploads` 目录不存在，则自动创建。
5. 使用 `uuid` 生成新的保存文件名，避免原文件名冲突。
6. 将上传文件保存到本地 `uploads` 目录。
7. 根据文件类型解析正文：
   - PDF：使用 `fitz` 打开文件，逐页提取文本。
   - DOCX：使用 `python-docx` 读取段落文本。
   - TXT：优先按 `utf-8` 解码，失败后按 `gbk` 解码。
8. 去除首尾空白，得到 `raw_text` 和 `cleaned_text`。
9. 对合同正文计算 SHA-256 哈希，得到 `content_hash`。
10. 向 `contracts` 表插入合同记录。
11. 初始审查状态设置为 `pending`。
12. 返回新合同 ID 和合同基础信息。

成功返回：

```json
{
  "code": 200,
  "message": "合同上传成功",
  "contract_id": 1,
  "title": "北京市住房租赁合同",
  "contract_type": "housing_lease",
  "status": "pending"
}
```

失败返回包括：

- `用户不存在`
- `目前只支持PDF、DOCX、TXT文件`
- `合同解析失败`

这个接口是合同业务的入口。只有合同成功上传并解析出文本后，后续详情查看和 AI 审查才有可用数据。

### 3.5 查询合同列表

接口：

```http
GET /contracts?user_id=1
```

查询参数：

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `user_id` | int | 是 | 当前登录用户 ID |

核心作用：查询当前用户上传过的所有合同。

处理流程：

1. 连接 MySQL。
2. 从 `contracts` 表查询当前用户的合同列表。
3. 查询字段包括 `id`、`title`、`contract_type`、`status`、`overall_risk`、`created_at`。
4. 按 `id DESC` 排序，最新上传的合同排在前面。
5. 如果合同类型是 `housing_lease`，补充中文类型名 `房屋租赁合同`。
6. 返回合同数组。

成功返回：

```json
{
  "code": 200,
  "message": "查询成功",
  "data": [
    {
      "id": 1,
      "title": "北京市住房租赁合同",
      "contract_type": "housing_lease",
      "contract_type_name": "房屋租赁合同",
      "status": "pending",
      "overall_risk": null,
      "created_at": "2026-08-19T16:34:46"
    }
  ]
}
```

合同列表页主要依赖这个接口展示合同卡片，包括合同名称、类型、上传时间、审查状态和综合风险。

### 3.6 查询合同详情

接口：

```http
GET /contracts/{contract_id}?user_id=1
```

路径参数：

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| `contract_id` | int | 合同 ID |

查询参数：

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `user_id` | int | 是 | 当前登录用户 ID |

核心作用：查询单份合同的元信息和正文内容。

处理流程：

1. 根据 `contract_id` 和 `user_id` 查询 `contracts` 表。
2. 如果没有查到记录，返回 `合同不存在`。
3. 如果合同类型是 `housing_lease`，补充中文类型名。
4. 优先使用 `cleaned_text` 作为展示正文。
5. 如果 `cleaned_text` 为空，则使用 `raw_text`。
6. 将最终展示文本放到响应字段 `content`。
7. 从返回对象里移除 `cleaned_text` 和 `raw_text`，避免前端同时处理多个正文字段。
8. 返回合同详情。

成功返回：

```json
{
  "code": 200,
  "message": "查询成功",
  "data": {
    "id": 1,
    "user_id": 1,
    "title": "北京市住房租赁合同",
    "contract_type": "housing_lease",
    "contract_type_name": "房屋租赁合同",
    "original_filename": "lease.pdf",
    "file_type": "pdf",
    "status": "pending",
    "overall_risk": null,
    "created_at": "2026-08-19T16:34:46",
    "content": "合同正文内容..."
  }
}
```

这个接口被两个页面使用：

- 合同详情页：展示合同信息和合同正文。
- 审查结果页：展示左侧或并列区域的合同原文。

### 3.7 发起合同审查

接口：

```http
POST /contracts/{contract_id}/review
Content-Type: application/json
```

路径参数：

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| `contract_id` | int | 合同 ID |

请求体：

```json
{
  "user_id": 1,
  "review_perspective": "neutral"
}
```

核心作用：将合同文本提交给外部 AI 审查服务，等待审查结果返回，并把结果写回数据库。

处理流程：

1. 根据 `contract_id` 和 `user_id` 查询合同。
2. 如果合同不存在，返回 `合同不存在`。
3. 查询当前用户邮箱，用于审查完成后的邮件通知。
4. 优先使用 `cleaned_text` 作为审查文本，若为空则使用 `raw_text`。
5. 如果合同没有可审查文本，返回 `合同没有可审查文本`。
6. 将合同状态更新为 `reviewing`，并清空之前的 `review_error`。
7. 组装审查请求数据，包含合同文本、合同类型和审查视角。
8. 以阻塞方式调用外部 AI 审查服务，后端超时时间为 900 秒。
9. 如果外部服务 HTTP 状态不是成功状态：
   - 将合同状态更新为 `failed`。
   - 将错误详情写入 `review_error`。
   - 返回 `code: 500`。
10. 如果外部服务返回的业务状态不是成功：
   - 将合同状态更新为 `failed`。
   - 写入失败详情。
   - 返回 `code: 500`。
11. 读取审查输出结果。
12. 如果输出结果是字符串形式的 JSON，则先解析成对象。
13. 从审查结果中提取 `overall_risk`，没有则默认 `low`。
14. 将合同状态更新为 `reviewed`。
15. 将综合风险、完整审查结果、审查时间写入 `contracts` 表。
16. 提交数据库事务。
17. 调用 `send_review_done_email()` 给用户发送审查完成邮件。
18. 返回完整审查结果给前端。

成功返回：

```json
{
  "code": 200,
  "message": "合同审查成功",
  "contract_id": 1,
  "status": "reviewed",
  "data": {
    "overall_risk": "medium",
    "review_summary": "审查总结",
    "summary": "补充说明",
    "risks": [],
    "missing_items": [],
    "conflicts": [],
    "positive_findings": []
  }
}
```

失败返回示例：

```json
{
  "code": 400,
  "message": "合同没有可审查文本"
}
```

或：

```json
{
  "code": 500,
  "message": "合同审查失败",
  "error": "具体异常信息"
}
```

这个接口是系统中耗时最长的接口。因为审查过程可能比较久，前端请求超时时间设置为 `900000` 毫秒，也就是 900 秒，和后端的 900 秒审查超时保持一致。

### 3.8 查询审查结果

接口：

```http
GET /contracts/{contract_id}/review?user_id=1
```

路径参数：

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| `contract_id` | int | 合同 ID |

查询参数：

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `user_id` | int | 是 | 当前登录用户 ID |

核心作用：从数据库读取某份合同的审查结果。

处理流程：

1. 根据 `contract_id` 和 `user_id` 查询 `contracts` 表。
2. 查询字段包括合同 ID、名称、类型、状态、综合风险、审查结果、审查错误和审查时间。
3. 如果合同不存在，返回 `合同不存在`。
4. 如果 `review_result` 存在：
   - 优先按 JSON 解析。
   - 如果解析失败，则直接返回原始内容。
5. 返回合同审查状态、综合风险、审查时间、错误信息和解析后的审查结果。

成功返回：

```json
{
  "code": 200,
  "message": "查询成功",
  "contract_id": 1,
  "title": "北京市住房租赁合同",
  "contract_type": "housing_lease",
  "status": "reviewed",
  "overall_risk": "medium",
  "reviewed_at": "2026-08-19T16:40:00",
  "review_error": null,
  "data": {
    "overall_risk": "medium",
    "review_summary": "审查总结",
    "risks": []
  }
}
```

这个接口只负责“读取结果”，不会重新触发 AI 审查。因此前端流程是先调用 `POST /contracts/{id}/review` 发起审查，审查成功后跳转到结果页，再调用 `GET /contracts/{id}/review` 读取已经保存的结果。

### 3.9 发送钉钉人工复核提醒

接口：

```http
POST /contracts/{contract_id}/dingtalk
Content-Type: application/json
```

请求体：

```json
{
  "user_id": 1
}
```

核心作用：把审查完成的合同摘要推送到钉钉群，提醒人工复核。

处理流程：

1. 根据 `contract_id` 和 `user_id` 查询合同。
2. 如果合同不存在，返回 `合同不存在`。
3. 判断合同状态是否为 `reviewed`，并且必须存在 `review_result`。
4. 如果合同还没审查完成，返回 `合同还没有完成AI审查`。
5. 解析数据库里的审查结果 JSON。
6. 读取前 3 条风险项。
7. 组装 Markdown 消息内容：
   - 合同名称
   - 综合风险
   - 主要风险
   - 审查总结
   - 人工复核提示
8. 从环境变量读取钉钉 webhook。
9. 调用钉钉 webhook 发送 Markdown 消息。
10. 根据钉钉返回的 `errcode` 判断是否发送成功。

成功返回：

```json
{
  "code": 200,
  "message": "已发送至钉钉群"
}
```

失败返回包括：

- `合同不存在`
- `合同还没有完成AI审查`
- `钉钉发送失败`

这个接口是审查结果页上的辅助功能，不影响合同审查本身。即使钉钉发送失败，已经保存的审查结果仍然存在。

## 四、后端核心业务流程

### 4.1 注册登录流程

注册流程：

1. 前端注册页输入 QQ 邮箱。
2. 点击发送验证码。
3. 前端调用 `POST /auth/send-code`。
4. 后端生成验证码，写入 `email_codes` 表，并发送 HTML 邮件。
5. 用户填写验证码和密码。
6. 前端调用 `POST /auth/register`。
7. 后端校验邮箱是否已注册、验证码是否存在、是否过期、是否匹配。
8. 校验通过后写入 `users` 表。
9. 前端提示注册成功并跳回登录页。

登录流程：

1. 前端登录页提交邮箱和密码。
2. 前端调用 `POST /auth/login`。
3. 后端查询 `users` 表。
4. 登录成功后返回 `user_id` 和 `email`。
5. 前端将 `user_id` 和 `email` 写入 `localStorage`。
6. 后续合同相关接口都从 `localStorage` 读取 `user_id`。

### 4.2 合同上传和解析流程

1. 用户进入上传页。
2. 前端填写合同名称，合同类型默认为 `housing_lease`。
3. 前端选择 PDF、DOCX 或 TXT 文件。
4. 前端使用 `FormData` 组装 `user_id`、`title`、`contract_type`、`file`。
5. 前端调用 `POST /contracts/upload`。
6. 后端验证用户存在。
7. 后端验证文件类型。
8. 后端将文件保存到 `uploads` 目录。
9. 后端根据文件类型解析正文。
10. 后端计算正文哈希。
11. 后端插入 `contracts` 表，状态为 `pending`。
12. 后端返回 `contract_id`。
13. 前端跳转到合同详情页。

### 4.3 合同审查流程

1. 用户在合同详情页点击“开始 AI 审查”。
2. 前端调用 `POST /contracts/{id}/review`。
3. 后端查询合同并读取正文。
4. 后端将状态改为 `reviewing`。
5. 后端调用外部 AI 审查服务，并等待结果。
6. 审查失败时，后端将状态改为 `failed`，保存错误信息。
7. 审查成功时，后端将状态改为 `reviewed`，保存综合风险、完整审查结果和审查时间。
8. 后端向用户邮箱发送审查完成通知。
9. 后端把审查结果返回给前端。
10. 前端跳转到审查结果页。
11. 审查结果页再次调用 `GET /contracts/{id}/review`，从数据库读取最终结果并渲染。

合同状态流转：

```text
pending -> reviewing -> reviewed
pending -> reviewing -> failed
```

### 4.4 审查结果展示流程

1. 前端进入 `/contracts/:id/review`。
2. 页面并行请求：
   - `GET /contracts/{id}`：获取合同详情和原文。
   - `GET /contracts/{id}/review`：获取审查结果。
3. 页面展示综合风险、审查总结、风险数量。
4. 页面将 `risks` 渲染为风险卡片。
5. 页面额外展示缺失事项、冲突条款、正向发现。
6. 风险卡片底部只保留来源汇总，例如 `来源：北京市住房租赁合同、广东省住房租赁合同 等 5 个来源`，避免原始来源列表过长。

## 五、前端请求方式与调用流程

### 5.1 前端技术结构

前端位于 `ai-contract-review-frontend`，主要技术为：

- Vue 3：页面组件和响应式状态。
- Vue Router：登录、注册、合同列表、上传、详情、审查结果页面路由。
- jQuery：统一负责 HTTP 请求发包。
- Vite：开发服务器和 `/api` 代理。
- Lucide：页面图标。

前端路由包括：

| 路由 | 页面 | 作用 |
| --- | --- | --- |
| `/login` | `LoginView.vue` | 用户登录 |
| `/register` | `RegisterView.vue` | 用户注册 |
| `/contracts` | `ContractListView.vue` | 合同列表 |
| `/contracts/upload` | `ContractUploadView.vue` | 上传合同 |
| `/contracts/:id` | `ContractDetailView.vue` | 合同详情和发起审查 |
| `/contracts/:id/review` | `ContractReviewView.vue` | 审查结果 |

路由守卫会检查 `localStorage` 中是否存在 `user_id`。如果没有登录，访问合同页面会被重定向到 `/login`。

### 5.2 jQuery 发包封装

前端所有请求都经过 `src/api/request.js`：

```js
$.ajax({
  timeout: API_TIMEOUT,
  dataType: 'json',
  ...options,
  url: `${API_BASE_URL}${options.url}`,
  success(response) {
    if (response?.code != null && Number(response.code) !== 200) {
      reject(new Error(response.message || '请求失败，请稍后重试'))
      return
    }

    resolve(response)
  },
  error(xhr, textStatus) {
    // 统一错误处理
  },
})
```

这个封装有几个关键点：

1. 请求库固定使用 jQuery 的 `$.ajax`。
2. `API_BASE_URL` 在开发环境默认是 `/api`。
3. 生产环境默认是 `http://127.0.0.1:8888`。
4. 可以通过 `VITE_API_BASE_URL` 覆盖接口地址。
5. 超时时间是 `900000` 毫秒，等于 900 秒。
6. 后端虽然通常 HTTP 状态是 200，但业务是否成功由响应体 `code` 字段决定。
7. 如果响应体里有 `code` 且不是 `200`，前端会直接 `reject`，页面进入错误处理分支。
8. 如果 HTTP 请求失败、后端不可连接、请求超时或 FastAPI 参数校验失败，也会被统一转换成 `Error`。

### 5.3 开发环境代理

`vite.config.js` 中配置了代理：

```js
server: {
  port: 5173,
  proxy: {
    '/api': {
      target: 'http://127.0.0.1:8888',
      changeOrigin: true,
      rewrite: (path) => path.replace(/^\/api/, ''),
    },
  },
}
```

因此开发环境下浏览器看到的请求地址通常是：

```text
http://localhost:5173/api/contracts/1/review
```

但实际后端收到的是：

```text
http://127.0.0.1:8888/contracts/1/review
```

这就是前端截图中请求地址是 `localhost:5173/api/...`，但后端接口实际定义是 `/contracts/...` 的原因。

### 5.4 JSON 请求怎么发

认证接口和审查接口使用 JSON 请求体。

`src/api/auth.js` 中封装了一个 `postJson()`：

```js
apiRequest({
  url,
  type: 'POST',
  contentType: 'application/json; charset=UTF-8',
  data: JSON.stringify(data),
})
```

调用示例：

- `sendCode({ email })` -> `POST /auth/send-code`
- `register({ email, password, code })` -> `POST /auth/register`
- `login({ email, password })` -> `POST /auth/login`

合同审查也是同样方式：

```js
apiRequest({
  url: `/contracts/${encodeURIComponent(id)}/review`,
  type: 'POST',
  contentType: 'application/json; charset=UTF-8',
  data: JSON.stringify({
    user_id: Number(userId),
    review_perspective: 'neutral',
  }),
})
```

### 5.5 文件上传怎么发

合同上传使用 `FormData`，因为需要同时传普通字段和文件：

```js
const formData = new FormData()
formData.append('user_id', userId)
formData.append('title', title)
formData.append('contract_type', contract_type)
formData.append('file', file)
```

发包时必须设置：

```js
processData: false,
contentType: false
```

原因是 jQuery 默认会尝试把 `data` 转成查询字符串，而文件上传需要浏览器自己生成 `multipart/form-data` 边界。因此这里不能让 jQuery 处理数据，也不能手动固定 `Content-Type`。

### 5.6 GET 请求怎么发

合同列表、合同详情和审查结果查询使用 GET 请求。

示例：

```js
apiRequest({
  url: '/contracts',
  type: 'GET',
  data: { user_id: userId },
})
```

jQuery 会把 `data` 自动拼成查询字符串，因此后端会收到：

```text
/contracts?user_id=1
```

合同详情和审查结果同理：

```text
/contracts/1?user_id=1
/contracts/1/review?user_id=1
```

## 六、前后端接口对应关系

| 页面/功能 | 前端函数 | 请求方式 | 后端接口 |
| --- | --- | --- | --- |
| 发送验证码 | `sendCode()` | POST JSON | `/auth/send-code` |
| 注册 | `register()` | POST JSON | `/auth/register` |
| 登录 | `login()` | POST JSON | `/auth/login` |
| 合同列表 | `getContracts()` | GET Query | `/contracts` |
| 上传合同 | `uploadContract()` | POST FormData | `/contracts/upload` |
| 合同详情 | `getContract()` | GET Query | `/contracts/{contract_id}` |
| 开始审查 | `startReview()` | POST JSON | `/contracts/{contract_id}/review` |
| 查询审查结果 | `getReview()` | GET Query | `/contracts/{contract_id}/review` |
| 发送钉钉复核 | `sendContractDingTalk()` | POST JSON | `/contracts/{contract_id}/dingtalk` |

## 七、项目接口返回规范

本项目后端多数接口使用统一 JSON 结构：

```json
{
  "code": 200,
  "message": "操作结果说明",
  "data": {}
}
```

其中：

- `code`：业务状态码，`200` 表示成功。
- `message`：给前端展示或调试用的中文提示。
- `data`：主要业务数据，不是所有接口都有。
- `error`：失败时的具体错误信息，不是所有失败响应都有。

前端判断成功失败的核心不是 HTTP 状态，而是响应体里的 `code`。只要响应里存在 `code` 且不是 `200`，前端请求封装就会把它当作失败处理。

## 八、项目数据流总结

从用户视角看，完整业务闭环如下：

1. 注册账号：邮箱验证码接口发送 HTML 邮件，注册接口校验验证码并创建用户。
2. 登录系统：登录接口返回用户 ID，前端写入 `localStorage`。
3. 上传合同：上传接口保存文件、解析文本、写入合同表。
4. 查看合同：详情接口返回合同元信息和正文。
5. 开始审查：审查接口读取正文、更新状态、调用外部 AI 审查能力、保存结构化结果。
6. 邮件通知：审查成功后，后端自动发送 HTML 格式的审查完成邮件。
7. 查看结果：结果接口从数据库读取结构化审查结果，前端渲染风险卡片。
8. 人工复核：钉钉接口把主要风险和总结推送到群里，方便后续人工处理。

从系统角度看，`app.py` 承担了接口层、业务编排层和数据访问层三类职责。它没有拆分成多个模块，因此代码阅读路径比较直接：从接口装饰器进入函数，再沿着数据库查询、业务判断、外部调用、数据库更新和 JSON 返回一路看下去即可。

## 九、项目特点

1. 接口集中：所有后端接口都在 `backend/app.py`，适合演示和快速定位问题。
2. 返回清晰：接口统一返回 `code`、`message`、`data` 风格的 JSON。
3. 前端调用简单：前端所有请求都通过 jQuery `$.ajax`，页面不直接写原始请求细节。
4. 文件支持明确：合同上传只支持 PDF、DOCX、TXT 三类文件。
5. 状态流转明确：合同从 `pending` 到 `reviewing`，最后变为 `reviewed` 或 `failed`。
6. 审查结果可持久化：审查完成后结果写入数据库，结果页读取数据库，不依赖页面临时状态。
7. 通知能力完整：注册验证码邮件、审查完成邮件、钉钉人工复核提醒都已接入。
8. 超时设置统一：前端 jQuery 请求和后端审查请求都使用 900 秒，适配较长的合同审查过程。

## 十、汇报时可重点说明的亮点

1. 后端基于 FastAPI 提供 REST 风格接口，接口路径清晰，认证、合同和审查功能边界明确。
2. 合同上传接口不仅保存文件，还根据不同文件类型提取正文，为后续审查提供统一文本输入。
3. 审查接口有完整状态管理，能区分审查中、审查成功和审查失败，并把错误信息保存到数据库。
4. 审查结果不是只返回给当前页面，而是持久化到 `contracts.review_result`，后续可以重复查看。
5. 前端使用统一 jQuery 请求封装，集中处理接口地址、超时、业务错误和网络错误。
6. 开发环境使用 Vite `/api` 代理，浏览器访问前端端口即可调用后端，降低跨域和端口配置复杂度。
7. 审查完成后自动发送 HTML 邮件，用户即使等待时间较长，也能收到完成提醒。
8. 审查结果页支持钉钉推送，把主要风险发给人工复核群，形成 AI 初审加人工复核的闭环。
